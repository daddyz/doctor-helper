= link_to(content_tag(:span, '', class: 'glyphicon glyphicon-sort-by-attributes'), url_for(grid.param_name => grid.as_query.merge(:order => column.name, :descending => false)), :class => "asc")
= link_to(content_tag(:span, '', class: 'glyphicon glyphicon-sort-by-attributes-alt'), url_for(grid.param_name => grid.as_query.merge(:order => column.name, :descending => true )), :class => "desc")
